name: "Get Email"

author: "ryohidaka"

description: "GitHub Actions to get user public email address."

branding:
  color: green
  icon: send

inputs:

outputs:

runs:
  using: "composite"
  steps:
    - name: Checkout repository
      uses: actions/checkout@v4
